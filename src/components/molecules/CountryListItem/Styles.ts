import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  coutryListItemNameWrapper: {
    display: 'flex'
  },
  coutryListItemIconWrapper: {
    marginRight: '.5rem'
  },
  coutryListItemCapitalWrapper: {
    marginLeft: '1.75rem'
  }
});

export default useStyles;